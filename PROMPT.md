here are some features i want you to change, add or remove:

in albums tab, remove the element which shows number of tracks inside the album.
i want more options to sort albums. i want to be able to sort by title, artist, and date.
tracks of an album must only sort by track number, remove the sorting element and set default sort to be by track number.
add a one-shot option to current queue so it ends after the last track in queue. the only options now are repeat whole queue or repeat single track.
in now-playing screen, after clicking the 3 dot menu, remove options to play next or add to queue. put add to playlist option instead of the 3 dot menu, and remove that element. remove go to album and go to artist options, instead we should go to album when we click the album cover, and go to artist by clicking the artist name. remove the track info option.
in the album overview section after selecting an album, remove the bottom 3 options. (go to artist, go to album, and track info)
the artist tab should be a list view rather than grid view.
the default sort option inside the tracks tab should be by album, and remove track number option.
inside the album overview, just under the album name, add the number of tracks inside the album in a smaller font (like: 15 Tracks)
